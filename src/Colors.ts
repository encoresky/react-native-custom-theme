import {Theme} from './types';
/**
 * 1. This file contain color objects for light and dark theme.
 * 2. Define all your colors here, please keep in mind that you have to define
 *    each color for both dark and light theme.
 * 3. Each theme object must be type of Theme
 */

export const light: Theme = {
  mode: 'light',
  primary: '#4e9cdb',
  secondary: '#4e9cdb',
};

export const dark: Theme = {
  mode: 'dark',
  primary: '#0a5897',
  secondary: '#0a5897',
};
